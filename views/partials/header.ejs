<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="/css/style.css">
    <% if (typeof isAdminDashboard !== 'undefined' && isAdminDashboard) { %>
        <link rel="stylesheet" href="/css/admin.css">
    <% } %>
    <% if (typeof isClientDashboard !== 'undefined' && isClientDashboard) { %>
        <link rel="stylesheet" href="/css/client.css">
    <% } %>
    <!-- Professional Icons -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
</head>
<body>
    <nav>
        <div class="nav-container">
            <a href="/" class="logo">Verba Srl Automation</a>
            <ul class="nav-links">
                <li><a href="/auth/admin-login">Admin Login</a></li>
                <li><a href="/auth/client-login">Client Login</a></li>
                <li>
                    <button id="smoothScrollToggle" class="smooth-scroll-toggle" title="Toggle Smooth Scrolling">
                        <svg width="20" height="20" viewBox="0 0 24 24" fill="currentColor">
                            <path d="M12 2L3 7l9 5 9-5-9-5zM3 17l9 5 9-5M3 12l9 5 9-5"/>
                        </svg>
                    </button>
                </li>
            </ul>
        </div>
    </nav>

